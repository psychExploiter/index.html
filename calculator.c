#include <stdio.h>

int main() {
    int a, b;
    printf("please enter 2 values for additon :- ");
    scanf("%d %d", &a,&b);
    printf("addition of 2 numbers is= %d", (a+b));
    return 0;
}